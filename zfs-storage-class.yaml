apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-volumes
parameters:
  recordsize: "16k"
  compression: "off"
  dedup: "off"
  fstype: "zfs"
  poolname: "kube/volumes"
provisioner: zfs.csi.openebs.io
reclaimPolicy: Delete
volumeBindingMode: WaitForFirstConsumer
